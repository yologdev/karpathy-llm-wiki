Verdict: PASS
Reason: All test cases specified in the task are implemented correctly, covering every exported function in both `links.ts` and `citations.ts`. Tests align with actual source logic (regex patterns, set-based filtering, escaping), and build/lint/tests all pass.
