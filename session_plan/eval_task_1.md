Verdict: PASS
Reason: The refactoring correctly consolidates duplicated `process.env` reads by exporting `detectEnvProvider` from `config.ts` and using it in both `embeddings.ts` and `llm.ts`. A new `_createEmbeddingModel` helper eliminates repeated provider construction code. All 966 tests pass, the `EMBEDDING_MODEL` override is preserved, and two new tests verify config-file-only embedding paths. Minor behavioral difference when both ANTHROPIC_API_KEY and OPENAI_API_KEY are set simultaneously (detectEnvProvider picks anthropic first, falling through to config instead of finding OpenAI directly), but this is an extremely unlikely edge case with no practical impact.
