Verdict: PASS
Reason: All 10 tests pass, covering every required scenario for both `loadPageConventions` and `loadPageTemplates` (missing file, missing heading, section extraction with/without following headings, sub-heading inclusion). Implementation uses temp directories with explicit `schemaPath` as specified, with proper cleanup.
