Verdict: PASS
Reason: Both API routes correctly implement the spec (list + single-handle with 404 on zero activity), ContributorBadge renders trust dots with correct color thresholds and graceful API fallback, and the wiki page view integrates AuthorBadges as a client boundary component replacing the old plain-text authors. Build, lint, and tests all pass.
