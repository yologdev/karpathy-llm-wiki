Verdict: PASS
Reason: Pure refactor correctly implemented — frontmatter.ts (262 lines) and raw.ts (125 lines) extracted from wiki.ts with proper re-exports preserving all existing import paths, no circular dependencies, `export type` used for interfaces, and build+tests pass.
